{
  "name": "@triolet-example/sine-tone-bundle",
  "version": "1.0.0",
  "dependencies": {
    "@triolet-example/sine-tone": "file:..",
    "triolet.bundle": "^0.5.0",
    "triolet.driver.webaudio": "^0.2.0"
  },
  "devDependencies": {
    "browserify": "^12.0.1",
    "watchify": "^3.6.1"
  },
  "private": true,
  "scripts": {
    "build": "browserify src --outfile bundle.js",
    "watch": "watchify src --outfile bundle.js --verbose"
  }
}
